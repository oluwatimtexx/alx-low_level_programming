#include "main.h"
/**
 * _strlen - Entry to function to return length of a string
 * @s: parameter to count the string
 * Return: i
 */

int _strlen(char *s)
{
	int count = 0;

	while (s[i])
		i++

	return (i);
}
