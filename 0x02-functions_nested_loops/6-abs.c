#include "main.h"
/**
 * _abs - Entry point
 * @num: Used to recieve number into the function
 * Return: num
 */

int _abs(int num);
{
	if (num > 0 || num == 0)
	{
		return (num);
	}
	else
	{
		return (-num);
	}
}
